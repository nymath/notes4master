---
title: "Sample Geometry"
author: "Nanyi"
date: "2022-09-25"˜
output:
  html_document:
    toc: yes
    toc_depth: 4
    number_sections: yes
    theme: united
    highlight: tango
  word_document:
    toc: yes
    toc_depth: '4'
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(ggplot2)
library(plotly,quietly = TRUE)
```
## R Markdown
```{r}
xx <- rnorm(50)
yy <- rnorm(50)
df <- cbind(xx,yy)
fig <- ggplot(data=NULL,aes(x=xx, y=yy)) + 
    geom_point()+
  annotate("segment", x = 0, xend = -0.32*4, y = 0, yend = 0.95*4, colour = "pink", size=1, alpha=0.6, arrow=arrow()) + 
  annotate("segment", x = 0, xend = -0.95*4, y = 0, yend = -0.32*4, colour = "pink", size=1, alpha=0.6, arrow=arrow())
ggplotly(fig)
```


```{r}
S = cov(df)
S
eigen(S)
```
